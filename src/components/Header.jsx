import React from 'react';

function Header () {
    return(
        <header>F22 Demo List Thing </header>
    );
};

/* can also be written as an arrow function
const Header = () => {
    return (
        <header></header>
    )
}
*/

export default Header;